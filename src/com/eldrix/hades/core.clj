(ns com.eldrix.hades.core
  "Public library API for Hades.

  This namespace is intentionally empty. The Hades public API has not yet
  been stabilised; internal implementation lives under
  `com.eldrix.hades.impl.*` and the CLI entry point at
  `com.eldrix.hades.cmd.core`. Both are subject to breaking changes without
  notice until a public surface lands here.")
